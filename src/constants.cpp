#include <pushplan/constants.hpp>

#include <vector>

const bool clutter::FRIDGE = true;
const float clutter::MAX_PLANNING_TIME = 60.0;
const int clutter::WINDOW = 20;
const float clutter::GOAL_THRESH = 0.02;
const float clutter::RES = 0.01;
const int clutter::GRID = 8;
const int clutter::COST_MULT = 1000;
